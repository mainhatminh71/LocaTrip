export interface Stats8Props {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Center" → stlFiH1ul
   *   "Left" → yC79BMFCG
   *   "Phone" → fWA0qN91r
   *   "Right" → Mgtg4oLJi
   */
  variant?: 'Center' | 'Left' | 'Phone' | 'Right' | 'yC79BMFCG' | 'Mgtg4oLJi' | 'stlFiH1ul' | 'fWA0qN91r';
  /**
   * Number — pass as `d0Am8LJlP` not `number`.
   * @default "53%"
   */
  d0Am8LJlP?: string;
  /**
   * Text — pass as `z_w_Rcuib` not `text`.
   * @default "Skills"
   */
  z_w_Rcuib?: string;
  /**
   * Center point — pass as `hrgsnmsl7` not `centerPoint`.
   * @default true
   */
  hrgsnmsl7?: boolean;
  /**
   * For Tablet — pass as `nz_vJ7MHD` not `forTablet`.
   * @default true
   */
  nz_vJ7MHD?: boolean;
  /** Additional properties */
  [key: string]: unknown;
}
