export interface Primarytravel49Props {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Desktop Dark" → vKU8t3fnb
   *   "Phone Dark" → pAbrcX5KL
   *   "Phone Primary" → a6nq4qYg2
   *   "Desktop" → PSYyXkAMb
   */
  variant?: 'Desktop Dark' | 'Phone Dark' | 'Phone Primary' | 'Desktop' | 'PSYyXkAMb' | 'vKU8t3fnb' | 'a6nq4qYg2' | 'pAbrcX5KL';
  /**
   * Title — pass as `ICeZ1XzMB` not `title`.
   * @default "Tạo lịch trình tức thì"
   */
  ICeZ1XzMB?: string;
  onICeZ1XzMBChange?: string;
  /**
   * Icon — pass as `nXlOjCdEz` not `icon`.
   * @default {"identifier":"module:f8Q5x3tcvy2sgigvrEse/WjPolkbUODUXgM3l5QnQ/X5rbDZv9v.js:default","moduleId":"f8Q5x3tcvy2sgigvrEse"}
   */
  nXlOjCdEz?: string;
  /**
   * Link — pass as `a17_dlysY` not `link`.
   */
  a17_dlysY?: string;
  /** Additional properties */
  [key: string]: unknown;
}
