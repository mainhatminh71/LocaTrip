export interface Sectiontag78Props {
  /**
   * Variant
   * Friendly names map to internal IDs:
   *   "Dark" → exlhOGd7O
   *   "Light" → GcBcwB523
   */
  variant?: 'Dark' | 'Light' | 'exlhOGd7O' | 'GcBcwB523';
  /**
   * Image — pass as `Qj6JTE_1X` not `image`.
   */
  Qj6JTE_1X?: string;
  /**
   * Title — pass as `dg_MYFFcN` not `title`.
   * @default "Đội ngũ"
   */
  dg_MYFFcN?: string;
  ondg_MYFFcNChange?: string;
  /** Additional properties */
  [key: string]: unknown;
}
